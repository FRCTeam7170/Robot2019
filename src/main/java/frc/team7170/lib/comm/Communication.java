package frc.team7170.lib.comm;

public class Communication {
    // TODO: redo communication system (vers 1 == lib.networktables) to use property system
    // utilities for adding to existing communication protocols
    // idea for on the fly parameter tweaking: allow params to be adjusted, then when a "tweaking session" is over,
    // print overview of all changes so they may be hard-coded in
}
